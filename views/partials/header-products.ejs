<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Quiet on the Shirt | Your Favourite TV Sitcoms, in a Blueprint Pattern </title>
        <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
        <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
        <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;700&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.1/css/all.min.css">
        <link rel="stylesheet" href=  <%= page === 'index' ?  "/css/glide.core.min.css": '' %>>
        <link rel="stylesheet" href=<%= page === 'index' ?  "/css/glide.theme.min.css": '' %>>
        <link rel="stylesheet" href=<%= page === 'index' ?  "/css/glide.css": '' %>>
        <link rel="stylesheet" type="text/css" href="/css/index.css">
        <link rel="stylesheet" type="text/css" href=<%= page === 'cart' ?  "/css/cart.css": '' %>>
        <link rel="stylesheet" type="text/css" href=<%= page === 'checkout' ?  "/css/checkout.css": '' %>>

    
      
      
    </head>
    <body>
        <header class="main-header">
        <nav id="navbar">
            <div class="container-nav">
                <a href="/">
                    <img id="logo2" src="/images/logo-short.png" alt="sweeping-petals-logo"><img id="logo1" src="/images/logo-text.png" alt="sweeping-petals-logo">
                </a>
                <a class="hide-desktop">
                    <i class="fas fa-bars menu" id="menu"></i>
                </a>
                <ul id="navbar-links" class="nav-links hide-mobile">
                    <li id="exit-btn" class="hide-desktop">
                        <i class="fas fa-times"></i>
                    </li>
                    <li>
                        <a href="/" class="nav-link" id="home-link">Home
                            <span class=<%= page === 'index' ? "navigation-border-active" : "navigation-border" %>></span>
                        </a>
                       
                    </li>
                    <li>
                        <a href="/cart" class="nav-link" id="cart-link">
                            Cart
                            <span>
                                <i class="fas fa-shopping-cart"></i>
                                <% if(cart) { %>
                                <span id="cart-counter"><%= !cart.products.length ? "0": cart.products.reduce((totalItems, product) => totalItems += product.quantity, 0) %>
                                </span>
                                <% }  %>
                                </i>
                            </span>
                            <span class=<%= page === 'cart' ? "navigation-border-active" : "navigation-border" %>></span>
                        </a>
                       
                      
                    </li>
                </ul>
            </div>
        </nav>
    </header>